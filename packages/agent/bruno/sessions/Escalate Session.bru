meta {
  name: Escalate Session
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/sessions/{{sessionId}}/escalate
  body: json
  auth: none
}

body:json {
  {
    "reason": "Agent is stuck on a complex merge conflict"
  }
}

docs {
  ## Escalate Session

  Manually escalate a session that needs human intervention.
  Transitions the session to the `escalated` state.

  **Request body:**
  - `reason` (optional) — Why the session needs escalation
}
