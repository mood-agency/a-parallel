meta {
  name: Cancel Session
  type: http
  seq: 6
}

post {
  url: {{baseUrl}}/sessions/{{sessionId}}/cancel
  body: none
  auth: none
}

docs {
  ## Cancel Session

  Cancels an active session. Stops the agent and cleans up resources.
  Transitions the session to the `cancelled` state.
}
