meta {
  name: List Sessions
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/sessions
  body: none
  auth: none
}

params:query {
  ~status: implementing
}

docs {
  ## List Sessions

  Lista todas las sesiones activas y completadas.

  **Query params (opcionales):**
  - `status` — Filtrar por estado: `created`, `planning`, `implementing`, `quality_check`, `creating_pr`, `waiting_ci`, `waiting_review`, `merging`, `merged`, `failed`, `escalated`, `cancelled`

  **Respuesta:**
  ```json
  {
    "sessions": [...],
    "total": 5,
    "active": 3
  }
  ```
}
